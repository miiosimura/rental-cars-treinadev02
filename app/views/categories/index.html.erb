<h3>Categorias</h3>

<% if @categories.empty? %>
  <p>Não existem categorias cadastradas no sistema</p>
<% end %>

<%= link_to 'Registrar nova categoria', new_category_path %><br/><br/>

<% @categories.each do |category| %>
  <li><%= link_to category.name, category_path(category) %></li>
<% end %>